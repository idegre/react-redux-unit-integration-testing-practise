import styled from 'styled-components'

export const FormHolder = styled.div`
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 50%;
    height: 50%;

    form {
        width: 100%;
    }
`